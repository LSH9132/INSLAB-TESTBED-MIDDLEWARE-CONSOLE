{
  "name": "inslab-testbed-middleware-console",
  "private": true,
  "scripts": {
    "dev:central": "pnpm --filter central-server dev",
    "dev:web": "pnpm --filter web-console dev",
    "dev:log": "pnpm --filter log-server dev",
    "build": "pnpm -r build"
  },
  "engines": {
    "node": ">=20"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "better-sqlite3",
      "cpu-features",
      "esbuild",
      "ssh2"
    ]
  }
}